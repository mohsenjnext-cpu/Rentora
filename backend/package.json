{
  "name": "rentora-backend-api",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node -e \"console.error('Production runtime is the Cloudflare Worker; use npm run dev for local frontend development.'); process.exit(1)\"",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
